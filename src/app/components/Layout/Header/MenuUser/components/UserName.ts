import styled from "styled-components";

const UserName = styled.span`
    font-size: 12px;
    color: #fff134;
    & span {
        color: #fff;
        font-size: 16px;
    }
`;

export default UserName;
