import styled from "styled-components";

const UserName = styled.span`
    font-size: 12px;
    color: #ed0b0b;
    & span {
        color: #fff;
        font-size: 16px;
    }
`;

export default UserName;
