import styled from 'styled-components';

const NavLinksWrapper = styled.ul`
    display: flex;
    align-items: center;
    list-style: none;
	margin: 0 auto;
	padding: 0;
`;

export default NavLinksWrapper;
