import styled from "styled-components";

const Text = styled.h1`
  padding: 10px;
  font-size: 16px;
  line-height: 16px;
  color: #fff;
  cursor: pointer;
  
  &:hover {    
    background-color: rgba(0, 0, 0, 0.04);    
  }
`;

export default Text;
