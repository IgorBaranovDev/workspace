import React from "react";

// styles
import { LogoWrapper, Text } from "./components";

const Logo: React.FunctionComponent = () => (
      <LogoWrapper>
        <Text>Workspace</Text>
      </LogoWrapper>    
  );

export default Logo;
