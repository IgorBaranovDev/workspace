import React from "react";

// styles
import { LogoWrapper, Text } from "./components";

const Logo: React.FunctionComponent = () => {
  return (
    <React.Fragment>
      <LogoWrapper>
        <Text>Workspace</Text>
      </LogoWrapper>
    </React.Fragment>
  );
};

export default Logo;
