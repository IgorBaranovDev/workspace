import styled from "styled-components";
import CircularProgress from "@material-ui/core/CircularProgress";

const MaterialCircularProgress = styled(CircularProgress)`
  color: #2b9cfc;
`;

export default MaterialCircularProgress;
