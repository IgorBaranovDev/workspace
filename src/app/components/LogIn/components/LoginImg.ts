import styled from "styled-components";
import { Avatar } from "@material-ui/core";

const LoginImg = styled(Avatar)`
  background-color: #299cff;
  padding: 28px;
  margin: 0 auto;
`;

export default LoginImg;
